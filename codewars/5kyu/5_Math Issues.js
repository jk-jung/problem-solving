Math.round = function (number) {
    return parseInt(number + 0.5);
};

Math.ceil = function (number) {
    return parseInt(number + 0.9999999999999);
};

Math.floor = function (number) {
    return parseInt(number);
};